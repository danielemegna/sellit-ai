[tool.poetry]
name = "sellitai"
version = "0.1.0"
description = "Sell It on the Web with AI"
authors = ["danielemegna"]
package-mode = true

[tool.poetry.scripts]
main = "sellitai.main:main"

[tool.poetry.dependencies]
python = "^3.11"
openai = "^1.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"